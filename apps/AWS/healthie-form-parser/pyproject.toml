[project]
name = "healthie-form-parser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.3",
    "syntrillo-lib",
]

[tool.uv.sources]
syntrillo-lib = { path = "../../../sources/syntrillo-lib", editable = true }
