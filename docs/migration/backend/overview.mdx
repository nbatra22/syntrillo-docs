---
title: "Backend (FastAPI)"
description: "FastAPI architecture, route structure, dependencies, and testing."
---

## Overview

The new backend is a FastAPI application located at `apps/FastAPI/`. It should replace the Flask server on PythonAnywhere.

The server runs via Uvicorn and also auto-generates API docs at `/docs` and `/redoc`.

## App Structure

```text
apps/FastAPI/
├── main.py           # App factory, CORS middleware, router registration
├── config.py         # Settings loaded from .env
├── dependencies.py   # Shared FastAPI dependencies (auth, pseudonyms)
├── routes/
│   ├── blood_pressure.py   # /api/v1/blood-pressure/*
│   ├── risk_score.py       # /api/v1/risk-score/*
│   └── devices.py          # /api/v1/devices/*
├── schemas/
│   └── blood_pressure.py   # Pydantic response models
├── tests/
│   ├── unit/               # No HTTP, no network — pure logic tests
│   └── integration/        # HTTP layer with mocked dependencies
└── syntrillo -> ../../sources/syntrillo   # symlink
```

---

## Stroke Risk Score

Basic Stroke Risk Score flow will look like:

Webhook (healthie update or internal db update) → will call our stroke risk score flow aggregate_data() and srs_update().

To view details about the current status of the Stroke Risk Score architecture and plans view:

<Columns cols={2}>
  <Column>
    <Card icon="sparkles" href="https://syntrillo.mintlify.app/srs-josh" title="SRS / JOSH">
      Click to view implementation plan and specs
    </Card>
  </Column>
  <Column>
    <Card icon="sparkles" href="https://syntrillo.mintlify.app/srs-nav" title="SRS / NAV">
      Click to view current status of stroke risk score updates
    </Card>
  </Column>
</Columns>

## Blood Pressure

Will be almost identical to our current blood pressure flow. Will add a visual representation of most recent reading + old readings.