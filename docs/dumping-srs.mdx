---
title: "dumping srs"
---

My next steps basically are:

currently I will keep agg_data the way it is and calculate risk score the way it is

Identify and confirm the all trigger events. so far i have:

- change lab values — healthie web hook integration
- new blood pressure value — pre-existing lambda function bp analysis lambda,
- changed medical history — internal forms
- change substance use etc. — internal forms

Only internal forms would have separate tables

one stroke risk score database final risk score, time it was calculated, each of the factors values

- one column which has the json response from agg_data

**Create the scheduler/event function** — a new Lambda that calls your existing `aggregate_data` + `calculate_risk_score` pipeline and writes results to a DB table

**Flask route should visualize values from the above db table** everytime an update to risk score has been triggered