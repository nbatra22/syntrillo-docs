---
title: "SRS / NAV"
---

<div style={{ textAlign: "right" }}>
  Updated {new Date().toLocaleDateString("en-US", {
    year: "numeric",
    month: "long",
    day: "numeric"
  })}
</div>

## Current status 

- **✅** create medical history, srs_names, and substance use tables
- <kbd>x</kbd> populate medical_history and substance_use from srs_form_responses or Healthie getAllFormResponses endpoint (currently got dummy data)
- **✅** make sure`medical_history` and `substance_use` are **append-only** db (keep full history with `created_at`) or **upsert/latest-only** (one row per patient)?
- <kbd>x</kbd>  write a new data_agg.py in stroke_risk_score_v3
  - **✅** add get_medical_history(patient-lookup-key) ← from new data table
  - **✅** add get_substance_use(patient-lookup-key) ← from new data table
  - <kbd>x</kbd> add get_blood_pressure (this can be the same way it is doing before)
  - <kbd>x</kbd> get_physical_activity (can also be the same)
  - <kbd>x</kbd> get_lab_values (can also be the same)
  - <kbd>x</kbd> get_heart_rate (can be the same)
- <kbd>x</kbd>  restructure data_agg output to be:

```text
def aggregate_data(self) -> dict:
    return {
        'medical_history': self.get_medical_history(),
        'blood_pressure': self.get_blood_pressure(),
        'lab_values': self.get_lab_values(),
        'substance_use': self.get_substance_use(),
        'physical_activity': self.get_physical_activity(),
        'heart_rate': self.get_heart_rate(),
    }
```

- <kbd>x</kbd>  start on srsCalculator (not here yet)  intermediate steps after talking w Josh
  - <kbd>x</kbd>  uses output from data_agg to calculate srs from algo in spreadsheet
- <kbd>x</kbd> Then can implement the engine class which uses Lambda trigger functions to update db and periodacally call data_agg and calculate_risk_score

### Basic SRS flow should be

Webhook (healthie update or internal db update) → call a engine class function like engine_run("trigger") or something which will run aggregate_data() followed by srs_update().

## Notes:

- Josh listed a 2 main event triggers to update our stroke risk score
  - submit onboarding form (done in healthie) // more than js the onboarding form
  - Or any of the risk factors are updated

    Agg_data should return a data object that looks like so

  ```text
  'data': {
  	'medical_history': {...}, // pulled from healthie and internal db
  	'blood_pressure': {...}, // pulled from internal db
  	'lab_values': {...}, // pulled from healthie
  	'substance_use': {...} // healthie and internal db
  	'physical_activity': {...}, // pulled from healthie - webhook for trigger
  	'heart_rate': {...} // pulled from healthie & tenovi
  }
  ```

when i say internal db i mean from srs_form_responses table

and pulled from healthie would be through api. ambigious on this because of the existence of healthie_form_responses table internally but saw that most of it was empty right now

I think the idea is that each of these groupings would have separate data tables within AWS that get updated from event triggers.

Agg_data pulls from each of these tables everytime one of them gets updated and then recalculates risk score

srs_data_table will looks like this:

```text
{
	'risk_score': {
		'score': FLOAT,
		'dependent_risk_values': {
			'value': FLOAT,
			'risk_factors': [...],
		},
		'independent_risk_values' {
			'value': FLOAT,
			'risk_factors': {
				'avg_sbp': FLOAT,
				'ldl': FLOAT,
				...
			}
		}
	},
	'priority_score': {
		'score': FLOAT,
		'dependent_risk_values': {
			'value': FLOAT,
			'risk_factors': [...],
		},
		'independent_risk_values' {
			'value': FLOAT,
			'risk_factors': {
				'avg_sbp': FLOAT,
				'ldl': FLOAT,
				...
			}
		}
	},
	'data': {...},
	'calculated_at': TIMESTAMP,
}
```

which in table format would look like

| Column                 | Data type                         |
| :--------------------- | :-------------------------------- |
| risk_score             | response from calculator function |
| priority_score         | response from calculator function |
| syntrillo_internal_key | UUID                              |
| data                   | JSON // response from agg_data    |
| calculated_at          | TIMESTAMP                         |
| trigger                | []                                |