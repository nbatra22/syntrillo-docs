---
title: "SRS / NAV"
---

## Current status

- **✅** create medical history, srs_names, and substance use tables
- <kbd>x</kbd> populate medical_history and substance_use from srs_form_responses or Healthie getAllFormResponses endpoint (currently got dummy data + Cris P Bacon)
- **✅** make sure`medical_history` and `substance_use` are **append-only** db (keep full history with `created_at`) or **upsert/latest-only** (one row per patient)?
- **✅**  write a new data_agg.py in stroke_risk_score_v3
  - **✅** add get_medical_history(patient-lookup-key) ← from new data table
  - **✅** add get_substance_use(patient-lookup-key) ← from new data table
  - **✅** add get_blood_pressure ← via tenovi
  - **✅** get_physical_activity ← get_srs_healthie()
  - **✅** get_lab_values ← get_lab_data()
  - **✅** get_heart_rate ← from tenovi
- **✅**  data_agg output looks like:

```text
{
  "patient_name": "Cris P. Bacon",
  "syntrillo_internal_key": "99fddf03-9304-4e48-8711-0cc4d825eb94",
  "last_updated": "2026-03-11T11:29:32.909832",
  "medical_history": {
    "has_stroke": 1,
    "stroke_count": "One",
    "stroke_mechanism": "Cryptogenic",
    "has_tia": 0,
    "tia_likelihood": null,
    "tia_mechanism": null,
    "has_prior_head_ct": 1,
    "prior_ct_date": "2024-01-15",
    "has_chronic_infarct": 0,
    "chronic_infarct_mechanism": null,
    "has_afib": 0,
    "has_id_anemia": 0,
    "anemia_severity": null,
    "arterial_clots": "Unknown",
    "venous_clots": "Unknown",
    "pfo_presence": "Unknown",
    "has_chf": 0,
    "ejection_fraction": "Unknown",
    "has_carotid_stenosis": 0,
    "stenosis_percentage": "Unknown",
    "has_osa": 1,
    "osa_severity": "Mild",
    "has_cad": 0,
    "cad_type": "Unknown",
    "has_valvular_heart_disease": 0,
    "has_ckd": 0,
    "has_hyperlipidemia": 1
  },
  "blood_pressure": {
    "systolic": {
      "trailing": {
        "SBP Count (>= 175)": 0.0,
        "variability": 2.9,
        "Avg SBP (mmHg)": 161.4,
        "Peak AVG SBP": 168.7
      },
      "baseline": {
        "SBP Count (>= 175)": 0.0,
        "Avg SBP (mmHg)": 110.7,
        "variability": 8.0,
        "Peak AVG SBP": 118.7
      }
    },
    "diastolic": {
      "trailing": {
        "Avg DBP (mmHg)": 102.0
      },
      "baseline": {
        "Avg DBP (mmHg)": 63.0
      }
    },
    "baseline_start_date": "2024-06-19 09:38:00-04:00"
  },
  "lab_values": {
    "ldl_value": 110.0,
    "hdl_value": 1.2,
    "creatintine_value": 2.1,
    "hgA1c_value": 10.2,
    "hsCRP_value": 2.2,
    "hemoglobin_value": 3.2
  },
  "substance_use": {
    "uses_alcohol": 1,
    "alcohol_frequency": "Occasionally",
    "alcohol_drinks_per_day": 1.0,
    "alcohol_drinking_days_per_month": 4.0,
    "alcohol_use_updated_at": null,
    "uses_tobacco": 1,
    "tobacco_type": "Cigarettes",
    "tobacco_status": "Former",
    "tobacco_cigarettes_per_day": 10.0,
    "tobacco_pack_years": 5.0,
    "tobacco_use_updated_at": null,
    "uses_marijuana": 0,
    "marijuana_frequency": null,
    "marijuana_use_updated_at": null
  },
  "physical_activity": {
    "inactivity_hours_answer": 8.0,
    "activity_minutes_answer": 60.0,
    "ssq_score": null
  },
  "heart_rate": {
    "average_rhr_baseline": 124.14,
    "average_rhr_trailing": null,
    "average_rhr_prior": null,
    "trailing_hr_average": null,
    "trailing_hr_variability": null
  }
}
```

- <kbd>x</kbd>  start on srsCalculator (not here yet)  intermediate steps after talking w Josh
  - <kbd>x</kbd>  uses output from data_agg to calculate srs from algo in spreadsheet
- <kbd>x</kbd> Then can implement the engine class which uses Lambda trigger functions to update db and periodacally call data_agg and calculate_risk_score

### Basic SRS flow should be

Webhook (healthie update or internal db update) → call a engine class function like engine_run("trigger") or something which will run aggregate_data() followed by srs_update().

## Notes:

- Josh listed a 2 main event triggers to update our stroke risk score
  - submit onboarding form (done in healthie) // more than js the onboarding form
  - Or any of the risk factors are updated

    Agg_data should return a data object that looks like so

  ```text
  'data': {
  	'medical_history': {...}, // pulled from internal db
  	'blood_pressure': {...}, // pulled from internal db
  	'lab_values': {...}, // pulled from healthie
  	'substance_use': {...} // healthie and internal db
  	'physical_activity': {...}, // pulled from healthie - webhook for trigger
  	'heart_rate': {...} // pulled from healthie & tenovi
  }
  ```

when i say internal db i mean from srs_form_responses table

and pulled from healthie would be through api. ambigious on this because of the existence of healthie_form_responses table internally but saw that most of it was empty right now

I think the idea is that each of these groupings would have separate data tables within AWS that get updated from event triggers.

Agg_data pulls from each of these tables everytime one of them gets updated and then recalculates risk score

srs_data_table will looks like this:

```text
{
	'risk_score': {
		'score': FLOAT,
		'dependent_risk_values': {
			'value': FLOAT,
			'risk_factors': [...],
		},
		'independent_risk_values' {
			'value': FLOAT,
			'risk_factors': {
				'avg_sbp': FLOAT,
				'ldl': FLOAT,
				...
			}
		}
	},
	'priority_score': {
		'score': FLOAT,
		'dependent_risk_values': {
			'value': FLOAT,
			'risk_factors': [...],
		},
		'independent_risk_values' {
			'value': FLOAT,
			'risk_factors': {
				'avg_sbp': FLOAT,
				'ldl': FLOAT,
				...
			}
		}
	},
	'data': {...},
	'calculated_at': TIMESTAMP,
}
```

which in table format would look like

| Column                 | Data type                         |
| :--------------------- | :-------------------------------- |
| risk_score             | response from calculator function |
| priority_score         | response from calculator function |
| syntrillo_internal_key | UUID                              |
| data                   | JSON // response from agg_data    |
| calculated_at          | TIMESTAMP                         |
| trigger                | []                                |